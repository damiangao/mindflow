id: meth_stdlib
name: Prefer Standard Library
description: Prefer mature standard libraries and tools, reduce dependencies
principles:
  - Standard library over third-party libraries
  - Mature tools over custom implementation
  - Reduce external dependencies
  - Use widely recognized solutions
evaluation_rule: Check if standard library or mature tools are used
weight: 0.8
status: active
